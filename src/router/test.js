import React from 'react'

class Test extends React.Component {
    state = {
        // 为了测试用方便，默认给一个值
        userName: 'myName is Lily'
    }

    render() {
        return (
            <div>
            
            </div>
        )
    }
}
export default Test